 <% if logged_in?(:site_admin) %>
   <nav class="breadcrumb blogcrumb">
   <%= link_to fa_icon('spinner'), toggle_status_blog_path(blog), style: blog_status_color(blog) %> 
   <%= link_to fa_icon('pencil'), edit_blog_path(blog) %> 
   <%= link_to fa_icon('trash-o'), blog, method: :delete, data: { confirm: 'Are you sure?' } %>
   </nav>
 <% end %>